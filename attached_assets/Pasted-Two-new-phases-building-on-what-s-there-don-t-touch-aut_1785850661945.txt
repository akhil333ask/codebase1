Two new phases, building on what's there — don't touch auth, RPM limiting, or the ReAct loop shape. The target behavior is documented in a live transcript: given the question "does DicomMediaIO.getUncacheImage call readRaster internally," the agent retrieved the exact chunk containing the answer (readRaster → getImageFragment → getUncacheImage) on its first read_file_chunk call, but never traced the chain — it saw a call to getUncacheImage at line 722 and never asked what method contains line 722. It then repeated near-identical read_file_chunk calls three more times without the stuck-loop guard catching it, and spent five <verify> retries hedging ("cannot be confirmed from available evidence") instead of investigating further. Fix that specific failure mode.

Phase 9 — wrapper-method continuation + graph-first call tracing.
In build_system_instructions, extend rule 13. Right now it tells the model to prefer query_graph over search_graph for relationship questions but doesn't say what to do once it has a method body in hand. Add: if a method's body is just a call (or return of a call) to another method, that is not a stopping point — the next method in the chain is now the new target, and investigation continues until a body that does real work is reached. State this using the readRaster → getImageFragment → getUncacheImage chain as the worked example, since that's the exact case we have evidence for.
Also reorder the preference inside that same rule: for "does A call B" questions, try one query_graph traversal over the CALLS edge (or whatever the real edge name is per that project's GRAPH SCHEMA block) before falling back to manually reading and chaining method bodies by hand — one Cypher traversal is cheaper than three speculative read_file_chunk calls and less likely to get lost the way this transcript did. Only fall back to manual chunk-reading if query_graph comes back empty or weak.

Phase 10 — verify failure triggers investigation, not regeneration.
check_verify_block currently only checks that a cited t-N id exists in tool_result_map — it doesn't check the citation actually supports the claim. Add a lightweight grounding check: reuse _significant_terms (already in the file, used by the weak-result heuristic) to confirm at least one significant term from the claim actually appears in the text of every tool result it cites. This is what let the model cite t-1 for "readRaster is a separate method" — technically valid, semantically empty.
Separately: when check_verify_block fails, start_interactive_chat's retry message currently just says "fix it." Change the retry message so it's specific about what to do, not just what's wrong — if the missing evidence concerns a call relationship, explicitly instruct the model to open the enclosing method around its most recent relevant read_file_chunk result rather than repeating the question to itself. The goal is to remove the decision point where the model chose to hedge five times in a row instead of reading one more chunk.

Also: extend the Phase 5b stuck-loop signature to treat read_file_chunk calls on the same file_path with overlapping line ranges as the same signature, not just byte-identical args — three retries with drifting 90-line windows over the same region should trip the guard.

Don't touch chunk size defaults (currently ~90–200 lines) — the transcript shows the model already had the answer inside the window it read; the fix is tracing what's there, not fetching more.